## 2025-02-21 - Accessible Sliders
**Learning:** Custom slider components must implement `role='slider'`, `tabIndex={0}`, `aria-valuenow`, and keyboard event handlers (Arrow keys) to be accessible.
**Action:** When optimizing or creating sliders, always ensure these attributes and handlers are present alongside visual updates.
